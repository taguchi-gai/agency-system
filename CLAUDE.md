# CLAUDE.md — このリポジトリで作業を始める前に読むこと

除菌消臭 事業管理システム（本部 ⇒ 施工代理店 ⇒ 取次店）の開発リポジトリ。
公開モック：https://taguchi-gai.github.io/agency-system/

## 現在地（2026-09-23 時点）

| 段階 | 状態 |
|---|---|
| ① モック（index.html）の機能作り込み | **一区切り**。本部回答③（現金・法人請求書払い）と取次機能（自社取次90%）まで反映済み。<br>技術的にロックしているわけではなく単なるHTMLなのでいつでも編集できる。ただし①複数台対応（申込↔案件を分ける構造変更）は
  モックで作ると本番アプリでもう一度作ることになるため**本番でだけ実装する**。②次のチャットの主目的はDB設計であり、
  モック改修に戻って脱線しない（このリポジトリで実際に一度脱線した経緯がある） |
| ② 全機能の連動テスト | **完了**。`tests/e2e.js`（52項目）と `tests/adversarial.js` が全合格。Playwright はローカルに導入済み |
| ③ 本番の技術選定 | **決定**：Node.js（Express）+ Supabase（PostgreSQL）+ Vercel。VPSは採用しない |
| ④ セキュリティ方針 | **決定**：カード情報は決済代行会社に渡し自社で持たない／RLSはDB側で強制 |
| ⑤ 本部への確認 | 第1回8項目は**回答受領済み**（②手数料率・④振込CSVのみ後送）。第2回7項目は**2026-09-23 送付・回答待ち**。<br>**回答を待たずに⑥へ進む**（下記「本部回答の扱い」参照） |
| ⑥ データベース設計（`docs/04_データベース設計.md`） | **未着手。次のチャットはここから始める** |
| ⑦ 新リポジトリ `agency-system-app` 作成 → 開発環境構築 | 未着手（⑥の後） |
| ⑧ バックエンド・フロントエンド実装 → **開発環境**にデプロイ | 未着手（⑦の後） |
| ⑨ 本部が開発環境で操作確認 → 問題なければ**本番環境**へデプロイ | 未着手（⑧の後） |

### 本部回答の扱い（回答が来る前に設計を進めるための約束）
- 回答待ちの項目は `docs/本部確認事項_2026-09-21.md` の「第2回 追加確認」の表にある。**各行に「仮の設定」を書いてあるので、
  DB設計はその仮の設定で進める。** 回答が来たら表に転記 → context.md 先頭に「確定」追記 → 設計書の該当箇所を直す。
- **設計の分岐点は③''（現金20%とカード80%の相殺）だけ。** 相殺の有無で精算の持ち方が変わるため、
  請求書と支払を別テーブルにし、相殺は精算処理のフラグで切り替えられる形にしておく（どちらの回答でも作り直しにならない）。
- ①'（法人請求書払いの事前承認）は「承認ステータス」欄を用意しておき、使うかどうかは回答で決める。
- ②手数料率・④振込CSVは値・出力形式の話で、テーブル構造には影響しない。

### 環境の流れ（決定）
開発環境で作る → 本部に開発環境を触ってもらう → 問題なければ本番環境へデプロイ。
開発環境と本番環境は **Supabase のプロジェクトと Vercel の環境をそれぞれ別に用意**し、本番のデータは開発環境に置かない。
開発環境にはモックと同様のサンプルデータを入れて本部が試せるようにする。

### ログインアカウント（決定）
- **施工代理店・取次店は、それぞれ自分のアカウント（ID／パスワード）でログインする。** 本部も本部のアカウントでログインする。
- モックの「ロールと店を選ぶだけのログイン」は本番では使わない。Supabase Auth で認証し、
  ログインユーザーに役割（本部／施工代理店／取次店）と所属（どの施工代理店・どの取次店か）を紐づける。
- **同じ施工代理店・取次店に複数の担当者アカウントを作れる**設計にしておく（1社1アカウントに固定しない）。
- 表示範囲の制御（自社・自店のデータだけ）は、このログイン情報を使って **RLS でDB側が判定**する。
- アカウントの発行は本部が行う（施工代理店・取次店を登録したときに初期アカウントを作る想定。詳細はDB設計で決める）。

## 次のチャットで最初にやること（この順で）

**このチャットの目的は `docs/04_データベース設計.md` を書き上げること。** モックの改修はしない。
利用者から「モックにも入れたい」と頼まれたら、「本番アプリで1回だけ作る方針（2026-09-23 合意）」を思い出してもらい、
それでも必要なら理由を聞いてから判断する。

1. このファイルを読む（自動で読まれる）。
2. `.claude/context.md` の先頭〜「★2026-09-21 追記（3）」までを読む（★2026-09-23 追記（2）〜（1）、★2026-09-22 追記 が最新）。
   それより下は履歴で、**古い決定（税抜価格・加盟店レイヤー等）が含まれるので現在の仕様として読まない**。
3. `.claude/plan.md` を全部読む。**第1〜7節＝支払方法、第8節＝複数台の申込（申込1:案件N）、第9節＝自社取次90%と取次機能、
   第10節＝仮の設定値。** DB設計はこの全部を取り込む。
4. `docs/02_技術仕様書.md` の第5節（DBスキーマ案）と5.5節（署名の保存方針）を読む。これが叩き台だが、
   plan.md の内容で大きく変わる（注文→申込＋案件に分割、決済手段・料率テーブル、請求書4種、取次窓口の selfOf 等）。
5. `docs/本部確認事項_2026-09-21.md` の「第2回 追加確認」の表を見て、回答が転記されているか利用者に聞く。
   **無ければ表の「仮の設定」で進める**（待たない）。
6. モックの `index.html` は**仕様の正**として参照する（データ構造は各 `let xxx = [` のサンプルとコメント、
   計算ロジックは `splitOf` / `payoutRows` / `billingGroups` / `createCashInvoice` 等）。読む量が多いので、
   必要な箇所だけ Grep で引く。
7. `docs/04_データベース設計.md` を書く。**大きな文書になるので、章ごとに書いてコミットする**（下記「設計書の構成案」）。
8. 設計が固まったら利用者に確認を取り、新リポジトリ `agency-system-app` を作る（環境名は付けない。現行リポジトリはモックとして残す）。
   リポジトリ作成は外部への操作なので**必ず事前に確認**する。

### 設計書（04_データベース設計.md）の構成案
1. 前提と方針（技術構成、環境分離、金額・日付の扱い、採番はDB側）
2. 主体：本部／施工代理店／取次窓口（`selfOf` で自社取次を表す）／法人顧客／担当者アカウント（Supabase Auth との紐づけ）
3. 申込と施工案件（**申込1:案件N**。決済・請求先・希望日は申込、施工代理店・施工日・署名・完了は案件。車両番号は案件）
4. 決済：決済手段、料率テーブル（手段×ブランド×有効期間）、申込単位の手数料と案件への按分、注文時点の確定値の書き込み
5. 配分と精算：案件ごとの配分（10/10/80、自社取次は90）、月次の支払、現金回収、相殺の切替（③''の仮設定＝相殺なし）
6. 請求書4種と明細（請求書1:案件N）、状態遷移、送付後の編集不可、領収書
7. 消耗品：商品・注文明細・在庫・入出庫・出荷ゲート・納品書
8. 契約：契約書、加盟金、電子契約の置き場所（版・署名記録。画面は次フェーズ）
9. 車種辞書（標準＋本部の手入力）
10. 通知（メール、将来LINE）とログ
11. RLS ポリシー：「誰がどのテーブルの何行を読み書きできるか」の表 → ポリシー
12. 開発環境／本番環境の分け方（Supabase プロジェクト2つ、Vercel、環境変数、サンプルデータ投入）
13. モックからの対応表（モックの変数・関数 → テーブル・列）

## 設計で必ず守ること（これまでの決定事項の要点）

- **金額はすべて税込・整数（円）**。施工料金：軽16,500／普通19,800／ワンボックス22,000／やに取り5,500。
- **配分**：（売上 − 決済手数料）× 本部10%／取次店10%／施工代理店80%。取次店・施工代理店を四捨五入で先に確定し、
  残り全額を本部にして合計を売上に一致させる。**案件ごとに計算してから合算**する。
- **支払対象は施工完了の案件のみ**。締めは受付日ではなく**施工完了日の月**。未完了分は参考表示のみ。
- **取次と施工が同じ会社の案件（自社取次）は取次分も施工代理店の取り分**で合計90%（本部10%）。判定は率の定数ではなく
  `isSelfServed(order)`（取次窓口の `selfOf` と案件の施工代理店が一致するか）で行う。自社取次窓口は独立した支払先にしない。
  取次機能は本部が施工代理店管理のボタンで付与し、停止しても窓口の記録は残す（過去案件の配分を壊さないため）。
  自社取次の申込は3営業日制限なし・自動で自社に割り当て。
- **現金は施工代理店が施工時に回収**し、80%を差し引いた20%を本部へ振込（本部→施工代理店の請求書で回収）。
  本部が取次店へ10%を振込。現金案件は「回収完了」を記録しないと施工完了を報告できない。詳細は `.claude/plan.md`。
- **決済手段（現金／PayPay／カードブランド／請求書払い）ごとに手数料率が異なり、改定される**。現金・請求書払いは0%の行として持つ。
  料率は有効期間つきの履歴で持ち、**注文発生時点の率と配分額をその注文に確定値として書き込む**。
  料率改定が過去の確定済み月に遡って影響してはならない。
- **消耗品注文は1注文に複数商品**（明細行）。送料は注文全体の商品代合計で判定（15,000円以上無料、未満1,000円）。
- **出荷できるのは請求書を送付した注文だけ**。判定は状態名ではなく「送付記録の有無」。出荷時に納品書を自動作成。
- **申込は1件で複数台**（申込1:施工案件N。`plan.md` 第8節で確定、モック未実装・本番で実装）。決済・請求先・希望日・現金の回収記録は
  申込単位、施工代理店の割り当て・施工日・署名・完了報告・車両番号は案件単位。決済手数料は申込の合計で計算して案件に按分。
- **請求書は4種**：消耗品（消耗品受発注の注文行から）／加盟金（契約書管理の契約行から）／現金回収（支払管理の「現金案件の精算」から。
  施工代理店宛て・締め月×施工代理店で1枚）／顧客請求（顧客請求画面から。法人顧客宛て・**月末締めで法人ごとに1枚**・支払期限は翌月末）。
  作成済み → 送付済み → 入金済。送付後は編集不可。作成入口は必ず元データのある画面に置き、請求書管理では作成しない。
- **採番はDB側で行う**（モックの件数ベース採番は本番では使わない）。番号の仮ルール：INV-年月-連番3桁、DN-年月-連番3桁。
- **行レベルセキュリティ（RLS）を必ずDB側で書く**。取次店は自店経由の案件だけ、施工代理店は自社の案件・自社が開拓した
  取次店・自社の注文・自社の契約だけ。画面側の絞り込みは利便性のためで、防御ではない。
- **カード情報は一切保存・通過させない**。決済画面は決済代行会社のもの（リダイレクトまたはトークン方式）に置き換える。
  モックの決済画面（`openPaymentModal`）は見た目だけのダミー。
- **利用者入力は表示時に必ずエスケープ**（モックでは `esc()`）。本番でも同じ原則。
- **日付はローカル日付で扱う**（`toISOString()` は UTC で日本では1日ずれる。モックの `fmtDate()` 参照）。
- **将来要件に余白を残す**：電子契約（条項確認＋署名、ひな形差し込み）、メール／LINE通知、名刺・フライヤーの自動生成。
  詳細は context.md「追記（4）」。

## 本部から受領後に差し替える場所（モック内）

| 受領するもの | 差し替え先 |
|---|---|
| 会社情報 → **反映済み（口座番号だけ伏せ字。本番で実値）** | `ISSUER` |
| 振込CSVの正式フォーマット（後送待ち） | `TRANSFER_CSV_COLUMNS` と `downloadTransferCsv()` |
| 決済手数料率（後送待ち。手段・ブランド別に差あり） | `commissionSettings.paymentFeeRate`（本番では料率テーブルへ） |
| 請求書番号ルール → **仮ルールで確定** | `nextInvoiceNumber()` |
| 請求書・納品書の書式 | `renderInvoiceDocument()` / `renderDeliveryNoteDocument()` |

## 作業ルール

- ブランチ：`main` で直接作業し、変更のたびにコミットしてプッシュする（利用者から包括的に許可済み。GitHub Pages が main を公開している）。
- `index.html` を変更した場合のみ、コミット前に `tests/e2e.js` と `tests/adversarial.js` を実行し `FAILS: 0` と `errors: []` を確認する。
  実行手順：`node <静的サーバー> 8142 &` → `node tests/e2e.js 8142`（Python は Store のスタブで使えない。
  `.claude/launch.json` の `mock` 設定で Node 製の簡易サーバーを起動できる）。設計書だけの変更ならテスト不要。
- 決定事項は `.claude/context.md` の**先頭に**「★日付 追記」として追加する（下に足さない）。
- **長い作業の前・区切りの良いところで、新しいチャットに切り替えることを利用者に提案する**。
  要約（圧縮）が途中で走ると作業が途切れるため。大きな作業は先に計画をファイルに書き、小刻みにコミットする。
- 分からないことは分からないと明示し、「こういう可能性がある」という形で提示する（利用者の希望）。
- 利用者に見せる文章は日本語。技術用語は初出で平易に言い換える。

## ファイル構成

```
index.html              モック本体（画面・ロジック・サンプルデータ、約2,700行）。仕様の正として参照。
                        技術的なロックはないが、複数台対応は本番でのみ実装（理由は「現在地」表参照）
car-dictionary.js       車種辞書（標準約160車種＋利用者追加分は localStorage）
tests/e2e.js            全フローの連動テスト（52項目）
tests/adversarial.js    抜け道・状態持ち越し・入力エスケープの検証
tests/README.md         テストの実行手順
docs/01_機能要件書.md    地理的マッチング機能の要件
docs/02_技術仕様書.md    API・DBスキーマ案・署名保存方針（第5節・5.5節が設計の叩き台）
docs/03_実装計画.md      フェーズ計画
docs/04_データベース設計.md  ← 次のチャットで作る
docs/本部確認事項_2026-09-21.md  本部への確認事項（第1回8項目＝回答済み、第2回7項目＝回答待ち・仮の設定つき）
docs/本部向け報告_2026-09-23.md  2026-09-23 に送付した報告メール本文（第2回確認の原文）
.claude/context.md      決定事項の時系列ログ（先頭が最新）
.claude/plan.md         支払方法／複数台申込／自社取次の設計方針（DB設計の入力）
.claude/audit.md        監査結果（修正した不具合・残した仕様）
.claude/launch.json     内蔵ブラウザ用の簡易サーバー設定（Node製）
package.json            Playwright（テスト用）。node_modules は .gitignore 済み
```

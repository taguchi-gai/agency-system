# コード監査（2026-09-21 開始）— 開発環境デプロイ前の整理

作業手順：①全機能の連動テスト → ②コード全読みで不要・矛盾を洗い出し → ③修正・削除しテストで確認 → ④main にコミット。
圧縮が走った場合はこのファイルと git log から再開する。

## ① 発見した不具合（要修正）
- [x] **日付計算のタイムゾーン不具合**（本番で必ず発生）：`addDays` / `addBusinessDays` / `addCarDictEntry` が
      `toISOString()`（UTC）で日付文字列を作っているため、日本時間では**1日前の日付**になる。
      例：JST で addDays(2026-06-11, 30) → 07-10（正しくは 07-11）。請求書の支払期限・申込フォームの最短希望日がずれる。
      → ローカル日付で整形する `fmtDate()` を作り置き換える。
- [x] サンプルデータの矛盾：PO-0004 は2商品（消臭プロ×12・タバコ×5）だが、入出庫履歴にはタバコ −5 が無く、
      タバコの在庫 80 のまま（本来 75）。
- [x] 納品書番号のサンプルが `DN-202605-000` から始まっている（採番関数は 001 から）。
- [x] 契約書と施工代理店の紐づけが**名前一致**（`c.entity === contractor.name`）で壊れやすい → `entityId` を持たせる。
- [x] car-dictionary.js 冒頭コメントの料金が税抜のまま（本文は税込に更新済み）。

## ② 不要コード・整理対象
- [x] `payeeBank()` 未使用 → 削除
- [x] `renderHqSo` の `flowHtml("施工完了").replace(/done/g,"")` → `flowHtml(null)` で同じ結果
- [x] `commissionSettings` は再代入しないので `const`
- [x] 状況の絞り込み選択肢を各画面で手書き → `SO_STATUS` を使う
- [x] 施工代理店の契約書画面：レイヤーのバッジ色が常に紫（本部画面と不一致）
- [x] 施工代理店ダッシュボード：年を変えたとき、その年に無い月が選択されたままになる → 月をリセット

## ③ 仕様として残す（変更しない・要相談）
- 本部ダッシュボードの「消耗品 累計売上」は累計表示（本部向けなので現状維持。当月にするか要確認）
- 本部「施工注文管理」の一覧には絞り込み・並べ替えが無い（他画面と不揃い。必要なら追加）
- `nextInvoiceNumber` / `nextDeliveryNoteNumber` は件数＋1 の仮採番。本番はDB側で採番する

## 進捗
- [x] 全コード読了・機械チェック（未使用識別子・未使用CSS・タイムゾーン）
- [x] 連動テスト（Playwright）ベースライン：50項目中 1 件 FAIL（割り当て一覧の「同一都道府県」表示）
- [x] 修正適用・再テスト：50項目すべて合格、JSエラーなし。Asia/Tokyo・UTC・Los Angeles で日付一致を確認
- [x] テストを `tests/e2e.js` として保存（デプロイ前に毎回実行する）
- [x] main にコミット・プッシュ

---

# 監査 第2回（2026-09-21・別視点で再検証）

第1回は「全機能が動くか」を確認した。第2回は「壊せるか（状態の持ち越し・ガードの抜け道・入力の扱い）」を
狙って検証し、**新たに8件**見つけてすべて修正した。検証は `tests/adversarial.js` に保存。

| # | 内容 | 影響 | 対応 |
|---|------|------|------|
| A | 売上管理で「全期間」を選ぶと即座に最新月へ戻る（未選択と全期間をどちらも `null` で表していたため） | 全期間の集計が見られない | `undefined`＝未選択 / `null`＝全期間 に分離 |
| B | **請求書を送らずに「入金済にする」を押すと出荷できてしまう** | 出荷制限の抜け道。本部が意図した運用が崩れる | 判定を状態名から「送付記録 `sentAt` の有無」に変更。未送付の請求書は入金済にせず警告 |
| C | ログインを跨いで絞り込みが残る。別の代理店に切り替えるとプルダウンは「すべて」表示なのに0件になる | 原因が分からない空表示 | ログイン・ログアウト時に `clearAllTableState()` |
| D | **利用者の入力がHTMLとして実行される**（氏名欄に `<img onerror=...>` でスクリプトが動作） | 本番では重大。取次店が他者の画面で任意のコードを実行できる | `esc()` を追加し、表示する利用者入力 **105箇所** を全てエスケープ |
| E | 採番が件数ベースのため、途中の1件が取り消されると番号が重複する | 注文番号の重複 | 既存の最大番号＋1（`nextSeqId`）に変更 |
| F | 請求書の「小計（税込）」が送料込みの金額で、合計と同額だった | 取引先に出す書類の誤記 | 商品代小計 → 送料 → 合計 の順に修正（納品書も同様） |
| G | モーダルを背景クリックで閉じても編集中データが残る | 実害は無いが不整合のもと | `closeModal()` で破棄 |
| H | 対応不可で差し戻しても施工確定日が残る | 再割り当て時に古い日付が残る | 差し戻し時に `scheduled` を消す |
| 追加 | 振込CSVで、名義にカンマが含まれると列がずれる | 銀行取込の失敗 | CSVの引用規則で全項目を囲む |

再検証：`tests/adversarial.js` は全項目期待どおり、`tests/e2e.js` は 50項目すべて合格、JSエラーなし。
